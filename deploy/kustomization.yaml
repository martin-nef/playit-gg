apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: playit

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

secretGenerator:
  - name: secret
    files:
      - playit.toml=playit.toml.json
